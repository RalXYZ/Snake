#pragma once

#include "maps.h"
#include"resource.h"

int mapNumber = 0;
int spawnNumber = 0;

maps mapResource[] = { (IDR_MAP00), (IDR_MAP01), (IDR_MAP02), (IDR_MAP03), (IDR_MAP04), (IDR_MAP05), (IDR_MAP06) };
